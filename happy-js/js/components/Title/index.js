function Title(css, textContent, smallContent, otherText) {
    return `<h1 style="${css[0] +css[1]}">
                ${textContent} 
                <small>${smallContent}</small>
                <em>${otherText}</em>
            </h1>`;    
}






//recebe uma string e navega entre os espaçamentos dessa string

//contar minuto 32 pq volta o rito bacana

/*Quando for explicar Array para um leigo,
Analogia das diferentes residencias - CASA _ Predio
*/