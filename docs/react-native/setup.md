With node.js installed, setup Expo project:

```bash
npx expo@latest init go-pizza-mobile
```

Create a empty tsconfig.json file. So, execute:

```bash
npx expo start
```

And install suggested packages.

After this, convert your App.js file to App.tsx.
